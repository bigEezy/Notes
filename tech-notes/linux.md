# Linux

Overview: Common Linux commands and tips.

Key commands:
- List: `ls -la`
- Find files: `find . -name "*.py"`
- Show processes: `ps aux` / `top`
- Permissions: `chmod`, `chown`

Resources:
- https://linux.die.net/man/
