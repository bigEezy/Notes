# Node.js

Overview: Server-side JavaScript runtime and tooling.

Key commands:

- Run: `node index.js`
- Package install: `npm install`
- Start script: `npm run start`

Notes:

- Use `nodemon` for auto-restart in development

Resources:

- https://nodejs.org/en/docs/
